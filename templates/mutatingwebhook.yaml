apiVersion: admissionregistration.k8s.io/v1beta1
kind: MutatingWebhookConfiguration
metadata:
  name: truststore-injector-webhook-cfg
  labels:
    app: truststore-injector
webhooks:
  - name: truststore-injector.bakito.ch
    clientConfig:
      service:
        name: truststore-injector-webhook-svc
        namespace: truststore-injector
        path: "/mutate"
      caBundle: ${CA_BUNDLE}
    rules:
      - operations: [ "CREATE", "UPDATE" ]
        apiGroups: [ "" ]
        apiVersions: [ "v1" ]
        resources: [ "configmaps" ]
    objectSelector:
      # should only be mutated when label is present. objectSelector is evaluated against both the oldObject and newObject
      matchLabels:
        "ch.bakito.truststore/enabled": enabled